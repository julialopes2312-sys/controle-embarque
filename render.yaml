services:
  - type: web
    name: controle-embarque
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: db
      mountPath: /data
      sizeGB: 1
